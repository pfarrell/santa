%h4
  #{model.name} epics
%h4
%table.table
  %tbody
    -epics.each do |epic,stories|
      %tr
        %td
          %a{href: "/epic/#{epic.name}"}
            =epic.name
        %td
          =stories.count
